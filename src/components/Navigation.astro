---
interface Props {
  currentPath: string;
  currentSearch?: string;
}

const { currentPath, currentSearch = '' } = Astro.props;

const navItems = [
  { label: 'DASHBOARD', href: '/', path: '/', match: 'exact' },
  { label: 'PESO', href: '/peso', path: '/peso', match: 'prefix' },
  { label: 'FINANZAS', href: '/finanzas', path: '/finanzas', match: 'prefix' },
  { label: 'COMIDAS', href: '/comidas', path: '/comidas', match: 'prefix' },
  { label: 'LISTA', href: '/lista', path: '/lista', match: 'prefix' },
  { label: 'CONFIG', href: '/config', path: '/config', match: 'prefix' },
];

function isItemActive(item: typeof navItems[0]) {
  if (item.match === 'exact') {
    return currentPath === item.path;
  }
  // prefix
  return currentPath === item.path || currentPath.startsWith(item.path + '/');
}
---

<nav class="nav">
  <div class="nav-inner">
    <div class="nav-brand">
      <span class="brand-dot"></span>
      <span class="brand-text">TCT</span>
    </div>
    <div class="nav-links">
      {navItems.map((item) => {
        const isActive = isItemActive(item);
        return (
          <a 
            href={item.href} 
            class:list={['nav-link', { active: isActive }]}
          >
            {isActive ? `[ ${item.label} ]` : item.label}
          </a>
        );
      })}
    </div>
    <div class="nav-actions">
      <button id="theme-toggle" class="theme-btn" aria-label="Toggle theme">
        <span class="theme-icon">◐</span>
      </button>
      <a href="/api/auth/signout" class="nav-link signout">SALIR</a>
    </div>
  </div>
</nav>

<!-- Mobile bottom nav -->
<nav class="nav-mobile">
  {navItems.map((item) => {
    const isActive = isItemActive(item);
    return (
      <a 
        href={item.href} 
        class:list={['nav-mobile-link', { active: isActive }]}
      >
        <span class="nav-mobile-label">{item.label}</span>
        {isActive && <span class="nav-mobile-dot"></span>}
      </a>
    );
  })}
</nav>

<script>
  function initTheme() {
    const toggle = document.getElementById('theme-toggle');
    const html = document.documentElement;
    
    // Load saved theme
    const saved = localStorage.getItem('tct-theme');
    if (saved) html.setAttribute('data-theme', saved);
    
    toggle?.addEventListener('click', () => {
      const current = html.getAttribute('data-theme');
      const next = current === 'light' ? 'dark' : 'light';
      html.setAttribute('data-theme', next);
      localStorage.setItem('tct-theme', next);
    });
  }

  // Run on initial load and after view transitions
  initTheme();
  document.addEventListener('astro:after-swap', initTheme);
</script>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: var(--black);
    border-bottom: 1px solid var(--border);
    height: calc(56px + env(safe-area-inset-top, 0px));
    padding-top: env(safe-area-inset-top, 0px);
  }

  .nav-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-lg);
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-xl);
  }

  .nav-brand {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .brand-dot {
    width: 8px;
    height: 8px;
    background: var(--accent);
    border-radius: 50%;
  }

  .brand-text {
    font-family: var(--font-mono);
    font-size: var(--body);
    font-weight: 700;
    color: var(--text-display);
    letter-spacing: 0.1em;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
  }

  .nav-link {
    font-family: var(--font-mono);
    font-size: var(--label);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--text-disabled);
    text-decoration: none;
    padding: var(--space-sm) 0;
    transition: color var(--duration-micro) var(--ease-out);
    white-space: nowrap;
  }

  .nav-link:hover {
    color: var(--text-primary);
  }

  .nav-link.active {
    color: var(--text-display);
  }

  .nav-actions {
    display: flex;
    align-items: center;
    gap: var(--space-md);
  }

  .theme-btn {
    background: none;
    border: none;
    cursor: pointer;
    padding: var(--space-xs);
    color: var(--text-secondary);
    font-size: 18px;
    transition: color var(--duration-micro) var(--ease-out);
    line-height: 1;
  }

  .theme-btn:hover {
    color: var(--text-display);
  }

  .signout {
    color: var(--text-disabled);
    font-size: var(--label);
  }

  .signout:hover {
    color: var(--accent);
  }

  /* Mobile bottom nav */
  .nav-mobile {
    display: none;
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: var(--black);
    border-top: 1px solid var(--border);
    height: calc(64px + env(safe-area-inset-bottom, 0px));
    padding-bottom: env(safe-area-inset-bottom, 0px);
    justify-content: stretch;
    align-items: stretch;
  }

  .nav-mobile-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-xs);
    text-decoration: none;
    flex: 1;
    min-height: 64px;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
    cursor: pointer;
    position: relative;
  }

  .nav-mobile-link:active {
    background: var(--surface-raised);
  }

  .nav-mobile-label {
    font-family: var(--font-mono);
    font-size: 9px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--text-disabled);
    transition: color var(--duration-micro) var(--ease-out);
    pointer-events: none;
  }

  .nav-mobile-link.active .nav-mobile-label {
    color: var(--text-display);
  }

  .nav-mobile-dot {
    width: 4px;
    height: 4px;
    background: var(--text-display);
    border-radius: 50%;
    pointer-events: none;
  }

  @media (max-width: 768px) {
    .nav {
      height: calc(48px + env(safe-area-inset-top, 0px));
    }
    .nav-links {
      display: none;
    }
    .nav-mobile {
      display: flex;
    }
  }
</style>
